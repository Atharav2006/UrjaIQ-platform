name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *' # Every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: |
          # Pinging Hugging Face Space to keep it awake
          curl -I https://gadeatharva233-urjaiq-api.hf.space/
